#include <stdio.h>
#include <stdlib.h>

int main() {
    // char a[5]={0},b[5]={0};
    // scanf("%c",a);
    // scanf("%s",b);
    // scanf("%c",b+1);
    // scanf("%c",b+2);
    // for (int i = 0 ; i<sizeof(a); i++ ){
    //     printf("[a[%d]%d b[%d]%d &a[i]+1_%d &a[i]+2_%d] \n",i,a[i],i,b[i],*(&a[i]+1),*(&a[i]+2));
    // }

    int a;
    printf("%d\n",a);
    scanf("%d",&a);
    printf("%d",a);
    return 0;
}